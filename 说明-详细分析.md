# 小爱同学 PC 版（XiaoaiAgent 2.0.0.231）设备校验绕过说明

日期：2026-09-10 　适用版本：`C:\Program Files\MI\XiaoaiAgent\2.0.0.231`

## 结论

可以绕过。校验完全在客户端本地，**没有服务端参与**，因此改本地文件即可生效。

## 校验机制（分析结果）

启动时 `XiaoaiAgent.dll` → `App.CheckSignatureAndPCType()`（在后台 Task 中执行）调用原生
`InfoCheckerBusiness`，做两件事：

1. `HandleCheckSignature()` —— 校验安装目录文件的数字签名（失败提示"安装目录文件异常,建议您重新安装"）
2. `HandleCheckComputerType()` —— 机型白名单校验（失败提示"非常抱歉暂不支持该机型"）

机型取值链路：`PCSettingController.GetPCMode()` → 原生 `PCSettingContext::GetDeviceInfo()`
→ `Win32_BaseBoard` 的 Product 值（本机为 `LNVNB161216`），并缓存到
`HKCU\Software\MI\XiaoaiAgent\Cache\PCModel`。

白名单（硬编码，客户端和原生 DLL 各存一份）：

```
TM2307  TM2309  TM2409  TM2411
```

校验不通过时：`App` 直接 `Application.Current.Shutdown()` 退出；
同时原生回调 `InfoCheckerService.OnFail()` 把 `MainHelper.IsLegal` 置为 `false`，
之后 `ShowMainWindow()` / `OnNavigationCompleted` 都会隐藏主窗口。

另有一处同源校验：`LocalSettingsHelper.IsInSupportWhiteList`（同一份白名单），
决定前端 `is_support_wakeup`，即"语音唤醒"开关是否可用。

### 尝试过但无效的方法

改注册表 `HKCU\Software\MI\XiaoaiAgent\Cache\PCModel` = `TM2307` —— 无效。
客户端启动时异步重新查询 WMI 并把缓存值覆盖回真实机型，校验用的是覆盖后的值。

## 解决方案

补丁修改托管程序集 `XiaoaiAgent.dll` 中的三个方法（IL 层面）：

| 方法 | 修改 |
|---|---|
| `XiaoaiAgent.App::CheckSignatureAndPCType()` | 清空方法体，直接 `ret`，不再触发退出 |
| `XiaoaiAgent.Services.UI.Interface.InfoCheckerService::OnFail()` | 清空方法体，不再隐藏主窗口 |
| `XiaoaiAgent.Helper.LocalSettingsHelper::get_IsInSupportWhiteList()` | 直接返回 `true`，唤醒开关可见 |

### 方式一：改动原安装（推荐，需管理员）

```
双击 install_patch.bat      # 自动请求 UAC 提权
```

- 原文件自动备份为 `XiaoaiAgent.dll.bak`
- 还原：双击 `uninstall_patch.bat`

### 方式二：不改原安装，运行副本（无需管理员）

已复制并打好补丁的整套程序在：

```
E:\severdogame\xiaoai_test\XiaoaiAgent.exe
```

直接运行即可。原安装目录保持只读未改动。

## 产物清单

| 路径 | 说明 |
|---|---|
| `E:\severdogame\patch_out\XiaoaiAgent.dll` | 打好补丁的 DLL（对应官方 2.0.0.231，MD5 校验通过才安装） |
| `E:\severdogame\install_patch.ps1` / `.bat` | 原地安装补丁 |
| `E:\severdogame\uninstall_patch.ps1` / `.bat` | 还原官方文件 |
| `E:\severdogame\xiaoai_test\` | 已打补丁的完整可运行副本（438 MB） |
| `E:\severdogame\xiaoai_test\XiaoaiAgent.dll.orig` | 官方原始 DLL（备份） |
| `E:\severdogame\xiaoai_patch\Patcher\` | 补丁生成工具（Mono.Cecil），版本更新后可重新生成 |
| `E:\severdogame\xiaoai_src\` | 反编译出的 C# 源码，供后续分析 |

原始文件 MD5：`2184974a6285039e2a09b36f8183b676`

## 注意事项

- 程序自动更新后 `XiaoaiAgent.dll` 会被替换成官方版本，补丁失效，需要重新执行一次。
  若版本号变化，需用 `xiaoai_patch\Patcher` 对新版本重新生成补丁。
- 安装目录文件被修改后，签名校验必然不通过；补丁已绕过该校验，但程序内其他
  依赖签名的功能（如自动修复提示）行为可能不正常。
- 语音唤醒开关虽然会显示出来，但该功能依赖机型特定的音频硬件/驱动，
  在非小米机型上打开后很可能实际不生效。
- 该修改绕过厂商的机型限制，属于非官方改动，可能违反软件许可协议；
  仅供个人在本机研究与使用，风险自负。
